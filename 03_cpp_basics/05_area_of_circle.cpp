#include <bits/stdc++.h>
using namespace std;
int main() {
    int radius;
    cout << "Enter the radius:";
    cin >> radius;

    float area;
    area = M_PI * radius * radius;
    cout << "Area of the circle is " << area;

    return 0;
}