#include <iostream>
using namespace std;
int main() {
    int base, height;
    cin >> base >> height;

    float area = 0.5 * (base * height);
    cout << "Area of the triangle = " << base * height << endl;

    return 0;
}